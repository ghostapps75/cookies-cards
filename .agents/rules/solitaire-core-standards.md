---
trigger: always_on
---

🃏 Solitaire & Contact Sheet Rules1. Image Slicing & Asset HandlingMathematical Precision: Before writing any CSS or JS for card rendering, the agent must calculate the exact pixel dimensions of each card based on the total contact sheet size ($Width_{Total} \div 13$ and $Height_{Total} \div 4$).No Clipping: Ensure background-position or canvas draw commands account for any gutter/padding between cards to prevent "glitchy" edges or seeing parts of adjacent cards.Aspect Ratio: Maintain a consistent 2:3 or 3.5:2.5 ratio for all card sprites to avoid stretching.2. Game Logic (Draw-3 & Movement)Stack Validation: Every move must be validated against Solitaire rules (alternating colors, descending rank) before updating the DOM/State.Draw-3 Mechanics: When drawing from the deck, the agent must track the visibility of the top card of the three and handle "re-flipping" the deck accurately when empty.State Recovery: Any logic change that affects the cardStack array must be paired with a "State Snapshot" so the user can undo a broken move.3. UI & InteractionMovement Feedback: All valid moves must include a smooth transition; invalid moves must "snap back" to the original position immediately.Mobile/Touch Ready: Ensure event listeners account for both mousedown and touchstart for the drag-and-drop logic.
